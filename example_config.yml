# Example config file for setting configuration values
# TwitAnalyzer looks in the local directory for a file named '.config'
#
# Values should be defined as key:value pairs

# Twitter API Keys
CONSUMER_KEY: KEY
CONSUMER_SECRET: SECRET
ACCESS_TOKEN: TOKEN
ACCESS_TOKEN_SECRET: TOKEN_SECRET
